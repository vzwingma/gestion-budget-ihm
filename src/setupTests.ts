// Setup file loaded by Vitest before each test file (see vite.config.ts `test.setupFiles`).
// Enables jest-dom matchers (toBeInTheDocument, toHaveTextContent, ...) with Vitest's `expect`.
import '@testing-library/jest-dom';
